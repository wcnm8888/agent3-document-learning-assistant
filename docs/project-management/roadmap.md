# 项目路线图

## 当前产品状态

文档学习助手的核心产品能力、文档生命周期、知识库可用性、UI 高保真恢复和 DOC-001 文档治理均已完成。当前全量测试基线为 95 项通过。当前没有获批准的活动任务；详情见 [当前任务](current-task.md)。

## 当前活动任务

| 优先级 | 任务 | 用户价值 | 当前状态 |
|---|---|---|---|
| — | 无 | 等待负责人从候选方向中选择 | `idle` |

DOC-001 已完成并归档。候选任务仍不构成执行授权。

## 候选后续任务

以下均未获批准，不代表执行授权。

| 优先级建议 | 候选任务 | 价值 | 依赖 | 主要风险与预计范围 |
|---|---|---|---|---|
| P1 | Gradio 上传进度 404 兼容修复 | 消除控制台噪音，提高上传链路可信度 | 隔离 Gradio 版本/兼容实验、完整浏览器回归 | 上游版本可能破坏 UI；小到中等 |
| P1 | CI 与本地质量门禁自动化 | 提交后自动运行测试、编译和文档检查 | DOC-001 完成、负责人确认 GitHub Actions 策略 | 权限、Secret 和外部成本；中等 |
| P2 | 检索质量评测与增强 | 提升长文档、术语和跨章节问题召回 | 冻结评测集和成本/延迟预算 | rerank、混合检索、MQE/HyDE 增加成本与复杂度；中大型 |
| P2 | 学习记录可用性增强 | 笔记搜索、导出、标签和重点复习 | 当前笔记/报告数据模型评审 | 易膨胀为学习管理系统；中等 |
| P3 | 项目元数据与开发体验收口 | 修正 `pyproject.toml` 早期描述，统一开发命令 | DOC-001 完成 | 直接用户价值较低；小型 |

推荐在 DOC-001 完成后先由负责人选择：修复 Gradio 404，或建立 CI。不要自动创建下一张任务卡。

## 已完成能力摘要

- PDF / Markdown 多文档解析、分块、Embedding 和 Qdrant 索引；
- 文档搜索、筛选、排序、详情和问答范围；
- 可追溯问答，区分 PDF 页码与 Markdown 章节/段落/行号；
- 会话隔离、学习笔记、统计和报告；
- 文档归档、删除、恢复、重新索引和一致性校验；
- 桌面、平板、移动端知识工作台与视觉恢复；
- 本地 Git 历史和远端 `main` 发布记录。
- DOC-001 文档权威治理、历史归档与 PR 交付。

完整历史不在本文件展开；已关闭任务和旧 UI 路线位于 `docs/archive/`，Git 历史继续保留原始过程。

## 明确非目标

除非负责人重新批准产品方向，否则不规划：

- 认证、用户系统、多租户；
- Neo4j 或知识图谱；
- 公网生产部署和多实例；
- 多模态文档；
- 复杂自主 Agent 规划；
- 切换当前 Embedding 模型或重建现有生产 points。

## 长期风险

- Gradio `upload_progress?upload_id=undefined` 404 尚未真正修复；
- SQLite 与 Qdrant 删除采用补偿一致性，而非分布式事务；
- 未配置 CI；
- 认证、多租户、公网部署和真实生产故障演练未执行；
- 外部模型服务的可用性、价格和速率限制可能变化。

## 路线图更新规则

- 只保留当前任务、候选任务、依赖和短完成摘要；
- 已关闭任务的详细过程进入归档，不继续追加在本文件；
- 候选任务必须由负责人选择后才能建立任务卡和执行；
- 测试、Git、PR、CI 和部署状态必须由可复现证据支持。
